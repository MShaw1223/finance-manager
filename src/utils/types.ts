export type usersData = {
  uid: number;
  username: string;
  email: string;
  user_password: string;
};

export type Params = {
  params: { id: number };
};

export type CardData = {
  cid: number;
  card_name: string;
};
